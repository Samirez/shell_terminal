# shell_terminal
Building a simple version of a shell commandline interface

## Requirements
- CMake 3.13+
- A C11-compatible compiler (e.g. GCC or Clang)

## Build

```sh
cmake -S . -B build
cmake --build build
```

## Run

```sh
./build/shell
```
